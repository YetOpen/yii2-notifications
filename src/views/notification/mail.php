<?php

use yii\web\View;

/*
 * This view is needed so that the app default layout is used when sending notification via the default
 * [EmailChannel::send()] method.
 */
/* @var $this View */
/* @var $content string */

echo $content;
